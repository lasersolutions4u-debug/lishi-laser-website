# 2026-08-03 工作日志

## 全站 WhatsApp 浮动按钮

- 用户确认将 About 页右下角的 WhatsApp 浮动按钮扩展到所有公开 HTML 页面。
- `public/` 下共 153 个 HTML 文件（包括 `_template.html`），现已全部包含一个 `whatsapp-float` 按钮。
- 使用号码：`https://wa.me/525572080065`（墨西哥 WhatsApp）。
- 新增 `add-whatsapp-float.py`，按语言从对应 About 页读取已验证的按钮 HTML，只补齐缺失页面，重复运行不会添加重复按钮。
- `_template.html` 已同步加入按钮，后续通过 `build-i18n.js` 生成首页时不会丢失。

## 验证

- 全站结构检查：`STRUCTURE_BAD=0`，每个 HTML 文件均恰好一个浮动按钮。
- 批处理脚本第二次运行：`Added WhatsApp floating button to 0 files.`
- `python -m py_compile add-whatsapp-float.py` 通过。
- `python -m unittest tests.test_site_integrity` 有 1 个历史失败：越南 About 标题大小写断言不一致；其余 12 项通过，与本次按钮改动无关。
- 本次未提交、未推送、未部署。
